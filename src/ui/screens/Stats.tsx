export function Stats() {
  return (
    <section aria-label="Stats">
      <div className="card">
        <h2 className="card-title">Telemetry (Local Only)</h2>
        <p className="placeholder">// sparklines + streak calendar boot in Phase 3</p>
      </div>
    </section>
  );
}
